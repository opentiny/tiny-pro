# Local development profile. It is selected by spring.profiles.default=local.
spring.datasource.url=jdbc:mysql://localhost:3306/tinypro_flyway_test?allowMultiQueries=true&serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8&autoReconnect=true&allowMultiQueries=true&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=${DATABASE_PASSWORD:111111}

spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=${REDIS_PASSWORD:000000}

jwt.secret=${JWT_SECRET:0Zi4SA==}
reject.start=${REJECT_START:false}
tinypro.jwt.secret=${jwt.secret}
tinypro.reject.start=${reject.start}

# Convenient local defaults; production uses application-prod.properties.
spring.flyway.baseline-on-migrate=${FLYWAY_BASELINE_ON_MIGRATE:true}
springdoc.api-docs.enabled=${ENABLE_SWAGGER:true}
springdoc.swagger-ui.enabled=${ENABLE_SWAGGER:true}
